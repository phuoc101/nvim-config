{
  "Limit angle between -pi and pi": {
    "prefix": ["pi", "theta"],
    "body": [
      "${1:angle} = ((${1:angle} + np.pi) % (2*np.pi)) - np.pi"
    ],
    "description": "Limit angle between -pi and pi"
  }
}
